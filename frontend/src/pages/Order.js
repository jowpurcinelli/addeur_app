import React from 'react';

interface Cargos {
  id: number,
  name: string,
  latitude: number,
  longitude: number,
}
interface Carriers {
  id: number,
  name: string,
  latitude:number,
  longitude: number,
}